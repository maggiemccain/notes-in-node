var person = {
    name: "Maggie",
};

person.age = 25;

person.name = "May";

console.log(person);

// node (or nodemon) inspect filename
// list(# of lines you want to view)
// n takes you to next line
// c makes the rest of the file run
// repl - read evaluation print loop --> like the console (type repl to go back to prior mode)

//  node (or nodemon) --inspect-brk filename
// go to chrome://inspect/#devices in chrome
// click 'Open dedicated DevTools for Node' link